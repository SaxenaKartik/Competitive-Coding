#include <bits/stdc++.h>
using namespace std;
#define ll long long 

ll a[100005];

int main()
{
	int t;
	scanf("%d", &t);

	while(t--)
	{
		ll n,s;
		scanf("%lld %lld", &n, &s);

		for(int i=1;i<=n;i++)
		{
			scanf("%lld", &a[i]);
		}

			
	}
}